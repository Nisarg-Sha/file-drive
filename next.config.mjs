/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "hallowed-panther-564.convex.cloud",
        },
      ],
    },
  };
  
  export default nextConfig;